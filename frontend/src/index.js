import React from 'react';  // Global para contrução de intercaces web e mobile
import ReactDOM from 'react-dom';   // tipos de interface que o react vai criar
import App from './App';  // importa as telas do frontend

ReactDOM.render(<App/>, document.getElementById('root'));
